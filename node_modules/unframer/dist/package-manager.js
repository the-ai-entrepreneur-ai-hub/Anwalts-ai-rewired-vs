"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPackageManager = getPackageManager;
exports.installPackagesBatch = installPackagesBatch;
const child_process_1 = require("child_process");
const util_1 = require("util");
const utils_js_1 = require("./utils.js");
const execAsync = (0, util_1.promisify)(child_process_1.exec);
async function getPackageManager(targetDir) {
    const { detect } = await import('@antfu/ni');
    const packageManager = await detect({ programmatic: true, cwd: targetDir });
    if (packageManager === 'yarn@berry') {
        return 'yarn';
    }
    else if (packageManager === 'pnpm@6' || packageManager === 'pnpm') {
        return 'pnpm';
    }
    else if (packageManager === 'bun') {
        return 'bun';
    }
    else if (packageManager === 'deno') {
        return 'deno';
    }
    else if (packageManager === 'npm' || packageManager === 'yarn') {
        return packageManager;
    }
    const userAgent = process.env.npm_config_user_agent || '';
    if (userAgent.startsWith('yarn')) {
        return 'yarn';
    }
    if (userAgent.startsWith('pnpm')) {
        return 'pnpm';
    }
    if (userAgent.startsWith('bun')) {
        return 'bun';
    }
    return '';
}
async function installPackagesBatch({ packageNames, cwd, isDev = false, }) {
    if (packageNames.length === 0) {
        return { success: true };
    }
    try {
        const packageManager = await getPackageManager(cwd);
        // If no package manager is detected, skip installation
        if (packageManager === '') {
            utils_js_1.spinner.error(`No package manager detected in ${cwd}, skipping installation of packages: ${packageNames.join(', ')}`);
            return { success: false, error: 'No package manager detected' };
        }
        const devFlag = isDev
            ? packageManager === 'npm'
                ? '--save-dev'
                : '-D'
            : '';
        const packagesStr = packageNames.join(' ');
        utils_js_1.spinner.info(`Installing packages with ${packageManager}: ${packagesStr}`);
        utils_js_1.spinner.start(`Installing packages: ${packagesStr}`);
        return new Promise((resolve) => {
            let command;
            let args;
            switch (packageManager) {
                case 'npm':
                    command = 'npm';
                    args = ['install', ...packageNames];
                    if (devFlag)
                        args.push(devFlag);
                    break;
                case 'yarn':
                    command = 'yarn';
                    args = ['add', ...packageNames];
                    if (devFlag)
                        args.push(devFlag);
                    break;
                case 'pnpm':
                    command = 'pnpm';
                    args = ['add', ...packageNames];
                    if (devFlag)
                        args.push(devFlag);
                    break;
                case 'bun':
                    command = 'bun';
                    args = ['add', ...packageNames];
                    if (devFlag)
                        args.push(devFlag);
                    break;
                default:
                    command = 'npm';
                    args = ['install', ...packageNames];
                    if (devFlag)
                        args.push(devFlag);
            }
            const child = (0, child_process_1.spawn)(command, args, {
                cwd,
                stdio: ['ignore', 'pipe', 'pipe'],
            });
            let hasOutput = false;
            child.stdout?.on('data', (data) => {
                hasOutput = true;
                const lines = data
                    .toString()
                    .split('\n')
                    .filter((line) => line.trim());
                lines.forEach((line) => {
                    if (line.trim()) {
                        utils_js_1.spinner.update(`Installing: ${line.trim()}`);
                    }
                });
            });
            child.stderr?.on('data', (data) => {
                hasOutput = true;
                const lines = data
                    .toString()
                    .split('\n')
                    .filter((line) => line.trim());
                lines.forEach((line) => {
                    if (line.trim()) {
                        utils_js_1.spinner.update(`Installing: ${line.trim()}`);
                    }
                });
            });
            child.on('close', (code) => {
                if (code === 0) {
                    utils_js_1.spinner.info(`Successfully installed packages`);
                    resolve({ success: true });
                }
                else {
                    const errorMessage = `Installation failed with exit code ${code}`;
                    utils_js_1.spinner.error(`Failed to install packages: ${errorMessage}`);
                    resolve({ success: false, error: errorMessage });
                }
            });
            child.on('error', (error) => {
                const errorMessage = error.message || 'Unknown error';
                utils_js_1.spinner.error(`Failed to install packages: ${errorMessage}`);
                resolve({ success: false, error: errorMessage });
            });
        });
    }
    catch (error) {
        const errorMessage = error.message || 'Unknown error';
        utils_js_1.spinner.error(`Failed to install packages: ${errorMessage}`);
        return { success: false, error: errorMessage };
    }
}
//# sourceMappingURL=package-manager.js.map